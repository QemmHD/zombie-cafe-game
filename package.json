{
  "name": "deadbeat-diner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deadbeat Diner — a web-native horror-comedy restaurant tycoon (Phaser 3 + TypeScript), inspired by the classic Zombie Cafe.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "export-content": "python3 Tools/export_content_json.py"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^4.1.10"
  }
}
